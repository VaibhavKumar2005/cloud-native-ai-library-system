# Core Frameworks
# Security: Upgraded to patched versions (CVE-2024-* and CVE-2025-* fixes)
Django>=5.1.7  # Was 5.0.2 - Multiple SQL injection and DoS vulnerabilities fixed
djangorestframework>=3.15.2  # Was 3.14.0 - XSS via break_long_headers fixed (CVE-2024-#21/#133)
python-dotenv==1.0.1
django-cors-headers==4.3.1
drf-spectacular>=0.27.0  # API documentation (Swagger/OpenAPI)
gunicorn>=23.0.0  # Was 21.2.0 - HTTP Request Smuggling vulnerabilities fixed (CVE-2024-1135)
wheel>=0.46.0  # Malicious wheel unpacking CVE-2024-38335 fixed
jaraco.context>=6.0.0  # Path traversal via malicious tar archives CVE-2024-6345 fixed

# AI & LLM (Azure OpenAI + LangChain + Fallback)
openai>=1.3.0  # Azure OpenAI + Groq fallback
langchain>=0.3.7
langchain-openai>=0.3.7  # Azure OpenAI integration for LangChain (upgraded from 0.1.0 - fixes module import)
langchain-community>=0.3.7
langchain-core>=0.3.15
langchain-text-splitters>=0.3.2
azure-search-documents>=11.4.0  # Azure AI Search vector search

# Database & Vector Store
psycopg2-binary==2.9.9
sqlalchemy>=2.0.0  # Required for PostgreSQL ORM
azure-storage-blob>=12.19.0  # For document storage (alternative to local filesystem)

# Experiment Tracking (MLflow)
mlflow>=2.10.0  # For benchmark tracking, model registry, and Azure ML integration

# Evaluation & Benchmarking (RAGAS)
ragas>=0.1.0  # LLM-based evaluation metrics (faithfulness, answer_relevancy, context_precision, context_recall)
datasets>=2.0.0  # Required by RAGAS for dataset handling

# Utilities
requests==2.32.5
pypdf>=5.5.0  # Was 5.1.0 - 15+ DoS CVEs via crafted PDFs fixed
tiktoken==0.8.0
numpy==1.26.4
scikit-learn>=1.3.0  # For semantic similarity scoring

# Observability & Documentation
django-prometheus
prometheus-client>=0.19.0  # Required for custom counters, histograms, and REGISTRY
drf-spectacular

# Security & Secrets
cryptography>=40.0.0  # Fernet encryption for field-level document protection (field_content)
hvac>=2.1.0  # HashiCorp Vault client (local mode)
azure-identity>=1.15.0  # Azure credential chain (cloud mode)
azure-keyvault-secrets>=4.7.0  # Azure Key Vault secret reader (cloud mode)
djangorestframework-simplejwt>=5.4.0  # Was 5.3.1 - Info disclosure fixed (CVE-2024-#22/#134)
django-csp

# Background Tasks (Celery)
celery>=5.3.0
redis>=5.0.0
django-celery-beat>=2.5.0  # For periodic task scheduling

# Testing
pytest>=7.4.0
pytest-django>=4.5.0

# Distributed Tracing (OpenTelemetry)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-django>=0.41b0
opentelemetry-instrumentation-httpx>=0.41b0
opentelemetry-instrumentation-requests>=0.41b0
