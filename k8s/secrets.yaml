# ⚠️  TEMPLATE: Replace base64 values with your actual secrets before applying.
#     Generate base64 values with: echo -n "your-value" | base64
#     In production, use Sealed Secrets, External Secrets Operator, or Azure Key Vault.
apiVersion: v1
kind: Secret
metadata:
  name: verirag-secrets
  namespace: verirag
  labels:
    app.kubernetes.io/part-of: verirag
type: Opaque
data:
  # echo -n "change-me-in-production" | base64
  DJANGO_SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
  # echo -n "admin" | base64
  POSTGRES_USER: YWRtaW4=
  # echo -n "change-me-in-production" | base64
  POSTGRES_PASSWORD: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
  # GOOGLE_API_KEY and GROQ_API_KEY must come from Vault/Azure Key Vault,
  # not from static Kubernetes Secret manifests checked into source control.
  # echo -n "root" | base64
  VAULT_TOKEN: cm9vdA==
