# Core Frameworks
Django==5.0.2
djangorestframework==3.14.0
python-dotenv==1.0.1
django-cors-headers==4.3.1
gunicorn==21.2.0

# AI & LLM (Google Gemini + LangChain + Fallback)
google-genai>=0.1.0
langchain>=0.3.7
langchain-google-genai>=2.0.4
langchain-community>=0.3.7
langchain-core>=0.3.15
langchain-text-splitters>=0.3.2
openai>=1.0.0  # Required for Groq/Llama-3 fallback logic

# Database & Vector Store
psycopg2-binary==2.9.9
pgvector==0.3.6
sqlalchemy>=2.0.0  # Required for PGVector connection

# Utilities
requests==2.32.5
pypdf==5.1.0
tiktoken==0.8.0
numpy==1.26.4

# Observability & Documentation
django-prometheus
prometheus-client>=0.19.0  # Required for custom counters, histograms, and REGISTRY
drf-spectacular

# Security & Secrets
hvac>=2.1.0  # HashiCorp Vault client (local mode)
azure-identity>=1.15.0  # Azure credential chain (cloud mode)
azure-keyvault-secrets>=4.7.0  # Azure Key Vault secret reader (cloud mode)
djangorestframework-simplejwt==5.3.1
django-csp

# Background Tasks (Celery)
celery>=5.3.0
redis>=5.0.0
django-celery-beat>=2.5.0  # For periodic task scheduling

# Distributed Tracing (OpenTelemetry)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp>=1.20.0
opentelemetry-instrumentation-django>=0.41b0
opentelemetry-instrumentation-requests>=0.41b0